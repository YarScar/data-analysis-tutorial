// Test setup for Vitest. Keep minimal for now.
// This file is executed before tests run (configured in vitest.config.js).

// Example: set a small global helper if needed later
global.__TEST__ = true
